//sum an array in parallel with MPI

#include <stdlib.h>
#include <stdio.h>

int main(int argc, char *argv[]) {
	int id;
	int p;

int main(int argc, char *argv[]) {
	int id;
	int p;
	int arr = 
	// Initialize MPI
 	MPI_Init(&argc, &argv);
 	MPI_Comm_rank(MPI_COMM
 		int arr[] = init_array();
	// Initialize MPI
 	MPI_Init(&argc, &argv);
 	MPI_Comm_rank(MPI_COMM_WORLD, &id);
 	MPI_Comm_size(MPI_COMM_WORLD, &p);
 	
 	
